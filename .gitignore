# Build artifacts produced by scripts/build-libjxl-xcframework.sh.
# vendor/libjxl.xcframework is referenced by the .xcodeproj but is not
# committed — regenerate it with the script. .build/ is the script's scratch
# work directory.
.build/
vendor/libjxl.xcframework/
# eugo-kb edge bearer — per machine, never committed (scaffolded by `eugo-skills init-mcp`)
.claude/eugo-mcp-headers.txt
# eugo-kb central instructions, refreshed per machine by the session hook (never committed)
.claude/rules/eugo-central.md
